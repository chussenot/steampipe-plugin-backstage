connection "backstage" {
    plugin  = "chussenot/backstage"

    # Base URL of the Backstage API
    base_url = "https://your-backstage-instance.com"

    # API token for authentication
    api_token = "your-api-token-here"
}
